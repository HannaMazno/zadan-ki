'use strict';




const telephonPrice = 400,
      taxFee = 0.08,
      bankBalanse = 2400,
      acsecorizePrice = 50,
      limit = 1500;
 let amount = 0;     

      function countTaxFee (amount) {
        amount = amount*taxFee;
        return amount;
      }
      
      function goAround () {
        return amount.toFixed(2);
      }
      
      while(amount < limit) {
        amount = amount + telephonPrice;
        if (amout < )
      }

      let a = 12.577456746847;
      console.log(a.toFixed(2));